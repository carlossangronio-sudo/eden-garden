# Configuration Eden Garden
# Copiez ce fichier vers .env et modifiez les valeurs

# Port du serveur (local uniquement)
PORT=3000

# Base de données PostgreSQL
# En local, utilisez une URL PostgreSQL locale ou un service comme Neon/Supabase
# Sur Vercel, cette variable est automatiquement définie par Vercel Postgres
POSTGRES_URL=postgresql://user:password@localhost:5432/edengarden

# Secret de session (générez une chaîne aléatoire de 64+ caractères)
# Utilisez: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=

# Environnement (development ou production)
NODE_ENV=development

# Admin par défaut (uniquement utilisé par le seeder)
ADMIN_EMAIL=admin@edengarden.fr
ADMIN_PASSWORD=

# Clé secrète pour le seed via API (à supprimer après usage)
# Générez: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SEED_SECRET_KEY=

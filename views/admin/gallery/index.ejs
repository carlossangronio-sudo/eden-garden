<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Galerie Photos - Eden Garden Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 min-h-screen">
    <header class="bg-gray-800 border-b border-gray-700">
        <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between">
            <div class="flex items-center gap-4">
                <a href="/admin" class="text-xl font-bold text-white hover:text-amber-400">Eden Garden</a>
                <span class="text-gray-500">|</span>
                <span class="text-gray-400">Galerie Photos</span>
            </div>
            <a href="/admin/logout" class="text-red-400 hover:text-red-300 text-sm">Deconnexion</a>
        </div>
    </header>

    <main class="max-w-6xl mx-auto px-4 py-8">
        <div class="flex items-center justify-between mb-8">
            <h2 class="text-2xl font-bold text-white">Galerie Photos</h2>
            <div class="flex gap-4">
                <a href="/admin" class="text-gray-400 hover:text-white text-sm">← Retour</a>
                <a href="/admin/gallery/new" class="px-4 py-2 bg-amber-600 text-white rounded-lg hover:bg-amber-500 text-sm font-medium">
                    + Ajouter une photo
                </a>
            </div>
        </div>

        <% if (success) { %>
        <div class="bg-emerald-500/10 border border-emerald-500/50 text-emerald-400 px-4 py-3 rounded-lg mb-6">
            Operation reussie
        </div>
        <% } %>

        <% if (images.length === 0) { %>
        <div class="text-center py-16 text-gray-500">
            <p class="text-lg mb-4">Aucune photo dans la galerie</p>
            <a href="/admin/gallery/new" class="text-amber-500 hover:text-amber-400">Ajouter votre premiere photo</a>
        </div>
        <% } else { %>
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
            <% images.forEach(image => { %>
            <div class="bg-gray-800 rounded-xl overflow-hidden border border-gray-700 <%= !image.isVisible ? 'opacity-50' : '' %>">
                <div class="aspect-square relative">
                    <img src="<%= image.imageUrl %>" alt="<%= image.title || 'Photo' %>"
                         class="w-full h-full object-cover">
                    <% if (!image.isVisible) { %>
                    <div class="absolute top-2 right-2 bg-red-500 text-white text-xs px-2 py-1 rounded">Masquee</div>
                    <% } %>
                </div>
                <div class="p-3">
                    <p class="text-white text-sm font-medium truncate"><%= image.title || 'Sans titre' %></p>
                    <p class="text-gray-500 text-xs"><%= image.category %></p>
                    <div class="flex gap-2 mt-3">
                        <form method="POST" action="/admin/gallery/<%= image.id %>/toggle" class="flex-1">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <button type="submit" class="w-full text-xs py-1 rounded <%= image.isVisible ? 'bg-gray-700 text-gray-300' : 'bg-emerald-600 text-white' %>">
                                <%= image.isVisible ? 'Masquer' : 'Afficher' %>
                            </button>
                        </form>
                        <a href="/admin/gallery/<%= image.id %>/edit" class="flex-1 text-center text-xs py-1 bg-blue-600 text-white rounded">Modifier</a>
                        <form method="POST" action="/admin/gallery/<%= image.id %>/delete" onsubmit="return confirm('Supprimer cette photo?')">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <button type="submit" class="text-xs py-1 px-2 bg-red-600 text-white rounded">X</button>
                        </form>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>
        <% } %>
    </main>
</body>
</html>

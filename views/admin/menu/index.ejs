<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion du Menu - Eden Garden Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 min-h-screen">
    <header class="bg-gray-800 border-b border-gray-700">
        <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between">
            <div class="flex items-center gap-4">
                <a href="/admin" class="text-xl font-bold text-white hover:text-amber-400">🌿 Eden Garden</a>
                <span class="text-gray-500">|</span>
                <span class="text-gray-400">Gestion du Menu</span>
            </div>
            <a href="/admin/logout" class="text-red-400 hover:text-red-300 text-sm">Déconnexion</a>
        </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 py-8">
        <div class="flex items-center justify-between mb-8">
            <h2 class="text-2xl font-bold text-white">Menu du Restaurant</h2>
            <div class="flex items-center gap-4">
                <a href="/admin" class="text-gray-400 hover:text-white text-sm">← Retour</a>
                <a href="/admin/menu/new"
                    class="px-4 py-2 bg-amber-600 text-white font-bold rounded-lg hover:bg-amber-500">+ Ajouter</a>
            </div>
        </div>

        <% if (success) { %>
            <div class="bg-emerald-500/10 border border-emerald-500/50 text-emerald-400 px-4 py-3 rounded-lg mb-6">✓
                Opération effectuée</div>
            <% } %>

                <% if (menuItems.length===0) { %>
                    <div class="bg-gray-800 rounded-xl p-8 border border-gray-700 text-center">
                        <div class="text-4xl mb-4">🍽️</div>
                        <h3 class="text-xl font-bold text-white mb-2">Aucun plat</h3>
                        <a href="/admin/menu/new"
                            class="inline-block px-6 py-3 bg-amber-600 text-white font-bold rounded-lg hover:bg-amber-500">Ajouter
                            un plat</a>
                    </div>
                    <% } else { %>
                        <div class="bg-gray-800 rounded-xl border border-gray-700 overflow-hidden">
                            <table class="w-full">
                                <thead class="bg-gray-700">
                                    <tr>
                                        <th class="text-left text-sm font-medium text-gray-300 px-6 py-4">#</th>
                                        <th class="text-left text-sm font-medium text-gray-300 px-6 py-4">Image</th>
                                        <th class="text-left text-sm font-medium text-gray-300 px-6 py-4">Titre</th>
                                        <th class="text-left text-sm font-medium text-gray-300 px-6 py-4">Catégorie</th>
                                        <th class="text-left text-sm font-medium text-gray-300 px-6 py-4">Prix</th>
                                        <th class="text-left text-sm font-medium text-gray-300 px-6 py-4">Visible</th>
                                        <th class="text-right text-sm font-medium text-gray-300 px-6 py-4">Actions</th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y divide-gray-700">
                                    <% menuItems.forEach(function(item) { %>
                                        <tr class="hover:bg-gray-700/50">
                                            <td class="px-6 py-4 text-gray-400">
                                                <%= item.position %>
                                            </td>
                                            <td class="px-6 py-4">
                                                <% if (item.imageUrl) { %>
                                                    <img src="<%= item.imageUrl %>" alt=""
                                                        class="w-16 h-16 object-cover rounded-lg">
                                                    <% } else { %>
                                                        <div
                                                            class="w-16 h-16 bg-gray-600 rounded-lg flex items-center justify-center text-gray-400">
                                                            📷</div>
                                                        <% } %>
                                            </td>
                                            <td class="px-6 py-4">
                                                <div class="text-white font-medium">
                                                    <%= item.title %>
                                                </div>
                                                <div class="text-gray-500 text-sm truncate max-w-xs">
                                                    <%= item.description %>
                                                </div>
                                            </td>
                                            <td class="px-6 py-4"><span
                                                    class="px-2 py-1 bg-gray-600 text-gray-300 text-xs rounded">
                                                    <%= item.category %>
                                                </span></td>
                                            <td class="px-6 py-4"><span class="text-amber-500 font-bold">
                                                    <%= item.price %>€
                                                </span></td>
                                            <td class="px-6 py-4">
                                                <% if (item.isVisible) { %><span class="text-emerald-400">✓</span>
                                                    <% } else { %><span class="text-gray-500">✗</span>
                                                        <% } %>
                                            </td>
                                            <td class="px-6 py-4 text-right">
                                                <a href="/admin/menu/<%= item.id %>/edit"
                                                    class="px-3 py-1 bg-blue-600 text-white text-sm rounded hover:bg-blue-500">Éditer</a>
                                                <form method="POST" action="/admin/menu/<%= item.id %>/delete"
                                                    onsubmit="return confirm('Supprimer ce plat ?')" class="inline">
                                                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                                    <button type="submit"
                                                        class="px-3 py-1 bg-red-600 text-white text-sm rounded hover:bg-red-500 ml-2">Supprimer</button>
                                                </form>
                                            </td>
                                        </tr>
                                        <% }); %>
                                </tbody>
                            </table>
                        </div>
                        <% } %>
    </main>
</body>

</html>
<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= isEdit ? 'Modifier' : 'Ajouter' %> un plat - Eden Garden Admin
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 min-h-screen">
    <header class="bg-gray-800 border-b border-gray-700">
        <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between">
            <div class="flex items-center gap-4">
                <a href="/admin" class="text-xl font-bold text-white hover:text-amber-400">🌿 Eden Garden</a>
                <span class="text-gray-500">|</span>
                <span class="text-gray-400">
                    <%= isEdit ? 'Modifier' : 'Ajouter' %> un plat
                </span>
            </div>
            <a href="/admin/logout" class="text-red-400 hover:text-red-300 text-sm">Déconnexion</a>
        </div>
    </header>

    <main class="max-w-2xl mx-auto px-4 py-8">
        <div class="flex items-center justify-between mb-8">
            <h2 class="text-2xl font-bold text-white">
                <%= isEdit ? 'Modifier le plat' : 'Nouveau plat' %>
            </h2>
            <a href="/admin/menu" class="text-gray-400 hover:text-white text-sm">← Retour au menu</a>
        </div>

        <% if (error) { %>
            <div class="bg-red-500/10 border border-red-500/50 text-red-400 px-4 py-3 rounded-lg mb-6">
                <%= error %>
            </div>
            <% } %>

                <form method="POST" action="/admin/menu/<%= isEdit ? menuItem.id + '/edit' : 'new' %>"
                    class="space-y-6">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="bg-gray-800 rounded-xl p-6 border border-gray-700 space-y-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-300 mb-2">Titre *</label>
                            <input type="text" name="title" value="<%= menuItem ? menuItem.title : '' %>" required
                                class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:border-amber-500">
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-300 mb-2">Description</label>
                            <textarea name="description" rows="3"
                                class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:border-amber-500"><%= menuItem ? menuItem.description : '' %></textarea>
                        </div>
                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-300 mb-2">Prix (€) *</label>
                                <input type="number" step="0.01" name="price"
                                    value="<%= menuItem ? menuItem.price : '' %>" required
                                    class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:border-amber-500">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-300 mb-2">Catégorie *</label>
                                <select name="category" required
                                    class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:border-amber-500">
                                    <option value="Plat" <%=menuItem && menuItem.category==='Plat' ? 'selected' : '' %>
                                        >Plat</option>
                                    <option value="Entrée" <%=menuItem && menuItem.category==='Entrée' ? 'selected' : ''
                                        %>>Entrée</option>
                                    <option value="Dessert" <%=menuItem && menuItem.category==='Dessert' ? 'selected'
                                        : '' %>>Dessert</option>
                                    <option value="Cocktail" <%=menuItem && menuItem.category==='Cocktail' ? 'selected'
                                        : '' %>>Cocktail</option>
                                    <option value="Boisson" <%=menuItem && menuItem.category==='Boisson' ? 'selected'
                                        : '' %>>Boisson</option>
                                    <option value="Chicha" <%=menuItem && menuItem.category==='Chicha' ? 'selected' : ''
                                        %>>Chicha</option>
                                </select>
                            </div>
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-300 mb-2">URL de l'image</label>
                            <input type="url" name="imageUrl" value="<%= menuItem ? menuItem.imageUrl : '' %>"
                                class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:border-amber-500"
                                placeholder="https://...">
                        </div>
                        <% if (isEdit) { %>
                            <div>
                                <label class="block text-sm font-medium text-gray-300 mb-2">Position</label>
                                <input type="number" name="position" value="<%= menuItem ? menuItem.position : 0 %>"
                                    class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:border-amber-500">
                            </div>
                            <% } %>
                                <div class="flex items-center gap-3">
                                    <input type="checkbox" name="isVisible" id="isVisible" <%=(!menuItem ||
                                        menuItem.isVisible) ? 'checked' : '' %> class="w-5 h-5 rounded">
                                    <label for="isVisible" class="text-gray-300">Visible sur le site</label>
                                </div>
                    </div>
                    <div class="flex justify-end gap-4">
                        <a href="/admin/menu"
                            class="px-6 py-3 bg-gray-700 text-white rounded-lg hover:bg-gray-600">Annuler</a>
                        <button type="submit"
                            class="px-6 py-3 bg-amber-600 text-white font-bold rounded-lg hover:bg-amber-500">
                            <%= isEdit ? 'Enregistrer' : 'Ajouter' %>
                        </button>
                    </div>
                </form>
    </main>
</body>

</html>